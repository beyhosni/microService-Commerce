package com.example.order.messaging;

// Lightweight container class for messaging-related helpers. The public record
// `OrderCreated` lives in `OrderCreated.java` to satisfy the single-public-class
// per-file rule.
final class OrderEvents {
  private OrderEvents() {}
}
